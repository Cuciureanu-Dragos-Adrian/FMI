n = int(input('n='))
val = [int(input('val=')) for i in range (n)]
p = int(input('p='))

s = [0] * (n + 1)


def bkt(k):
    for i in range(val[k - 1] + 1):
        s[k] = i
        if k < n:
            bkt(k + 1)
        else:
            if sum(s) >= p:
                print(*s[1:])


bkt(1)
